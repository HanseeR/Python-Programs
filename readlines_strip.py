#readlines
#readlines reads a file until the end of thefile is there and returns a list 

# file.readlines(hint)

# strip
str = 'pop sos pop sos'

print(str.strip())

print(str.strip('pop'))

print(str.strip('sos'))









